# Verification checklist

- Same active requests appear in the Service Location card and workspace.
- Request filtering uses the stable Service Location identity.
- Staff can acknowledge or resolve a request from the workspace.
- `close_account` remains a service request, not a payment action.
- No `paymentStatus`, `paidQuantity`, `registerTablePayment`, PSP, fiscal, settlement, or points authority is introduced.
